# Core Deep Learning Framework & Vision
torch>=2.0.0            # PyTorch framework for model training, evaluation, and tensor operations
torchvision>=0.15.0     # Computer vision utilities compatible with PyTorch

# Model Backbone Architecture
timm>=0.9.0             # PyTorch Image Models: provides the PVTv2-B2 Vision Transformer encoder backbone

# Data Augmentation & Image Processing
albumentations>=1.3.0   # Medical image data augmentation pipeline used in train/augment_kvasir.py
opencv-python>=4.7.0    # Used in augment_kvasir.py and predict_single_image.py (contours, heatmaps, color transforms)
Pillow>=9.0.0           # PIL image loading and resizing across dataset loaders and inference scripts

# Numerical Operations & Progress Tracking
numpy>=1.22.0           # Array manipulation & metric calculations (Dice, IoU, MAE, F-measure, S-measure)
tqdm>=4.64.0            # Progress bar visualization during Kvasir dataset augmentation
